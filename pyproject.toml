[project]
name = "mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Meghan Gillen", email = "meghs315@uw.edu" }
]
requires-python = ">=3.11"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "fastmcp>=2.3.0rc1",
    "requests>=2.34.2",
    "starlette>=1.6.0",
    "uvicorn>=0.52.4",
]

[project.scripts]
mcp = "mcp:main"

[build-system]
requires = ["uv_build>=0.12.8,<0.13.0"]
build-backend = "uv_build"
